{
  "name": "@chess100com/client-core",
  "version": "1.0.0",
  "private": true,
  "description": "Platform-agnostic client logic (stores, api, socket) for web and React Native.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check-ts": "tsc --noEmit"
  },
  "dependencies": {
    "@chess100com/rules": "*",
    "mobx": "^6.12.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
